def pyramid_name(name):
    n = len(name)
    for i in range(1, n + 1):
        # Print leading spaces
        print(" " * (n - i), end="")
        # Print characters from the name
        for j in range(i):
            print(name[j], end="")
        print()

# Print pyramid for name "deep"
pyramid_name("deep")

